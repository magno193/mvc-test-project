@model IEnumerable<Ciatecnica.Models.Client>
@{
	ViewData["Title"] = "Clientes | Index";
}

<section class="container p-3">
	<div class="row pt-4">
		<div class="col-9">
			<h2 class="text-dark font-weight-bold">Lista de Clientes</h2>
		</div>
		<a asp-controller="Client" asp-action="Create" class="col-3 btn btn-success font-weight-bold m-auto">
			<i class="fas fa-plus mr-1"></i>Criar novo Cliente
		</a>
	</div>
	<hr>

	@if (Model.Count() > 0)
	{
	 <table class="table table-bordered table-striped" style="width: 100%">
		<thead>
		 <tr>
			<th>
			 Nome/Razão Social
			</th>
			<th>
			 Sobrenome/Nome Fantasia
			</th>
			<th>
			 Tipo
			</th>
			<th>
			 CPF/CNPJ
			</th>
			<th>
			 CEP
			</th>
		 </tr>
		</thead>
		<tbody>
				@foreach (var item in Model)
				{
			 <tr>
				<td>@item.NameCorporateName</td>
				<td>@item.SurnameTradeName</td>
				<td>@item.Type</td>
				<td>@item.CpfCnpj</td>
				<td>@item.ZipCode</td>
			 </tr>
				}
		</tbody>
	 </table>
	}
</section>
